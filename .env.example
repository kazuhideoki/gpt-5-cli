# OpenAI API Key (required)
OPENAI_API_KEY=your-api-key-here

# OpenAI Models (optional)
# Defaults for variables: gpt-5, gpt-5-mini, gpt-5-nano
# Script default selection (no flags): uses OPENAI_MODEL_NANO
OPENAI_MODEL_MAIN=gpt-5
OPENAI_MODEL_MINI=gpt-5-mini
OPENAI_MODEL_NANO=gpt-5-nano

# History index file (optional)
# Default: history_index.json (repository root)
# Tilde (~) is supported. Example for iCloud Drive (macOS):
# GPT_5_CLI_HISTORY_INDEX_FILE=~/gpt-5-cli/history_index.json

# Prompts directory for system prompt templates (optional)
# Default: prompts (repository root)
# Tilde (~) is supported. Example:
# GPT_5_CLI_PROMPTS_DIR=~/gpt-5-cli/prompts

# Default reasoning effort (optional)
# Default: low
# OPENAI_DEFAULT_EFFORT=low  # one of: high|medium|low

# Default response verbosity (optional)
# Default: low
# OPENAI_DEFAULT_VERBOSITY=low  # one of: high|medium|low

# Disable colorized output (optional)
# NO_COLOR=1
