/**
 * @file finalize アクション実行ランタイムの仕様テスト（d2-html アクション周り）。
 */
import { describe, it } from "bun:test";

describe("executeFinalizeAction", () => {
  it("d2-html アクションで d2 コマンドを実行する", () => {
    // Step3 で実装
  });

  it("openHtml が有効な場合は HTML を規定アプリで開く", () => {
    // Step3 で実装
  });
});
