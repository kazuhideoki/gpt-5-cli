import { describe, it } from "bun:test";

describe("createCliToolLoggerOptions", () => {
  it.todo("CLI ロガーを通じてツールの情報ログを出力する");
  it.todo("debugEnabled が true の場合にデバッグログを流す");
  it.todo("debugEnabled が false の場合はデバッグログを登録しない");
});
